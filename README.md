# debian-clean-installed

git clone https://github.com/Menno-Gijzen/debian-clean-installed && cd debian-clean-installed && sudo chmod +x Clean-machine && sed -i 's/\r//' Clean-machine.sh && ./Clean-machine.sh
